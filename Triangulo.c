#include <stdio.h>
#include <math.h>

int main(){
    float hipotenusa
    float cateto1
    float cateto2 

    printf("Digite el primer cateto a: ");
    scanf (%f,&cateto1);
    printf("Digite el segundo cateto b: ");
    scanf (%f,&cateto2);

    hipotenusa = sqrt(pow(cateto1,2)+pow(cateto2,2));
    printf ("La hipotenusa del triángulo es: %f",hipotenusa);

    return 0;
}